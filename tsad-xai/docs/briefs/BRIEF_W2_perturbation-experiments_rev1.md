# 구현 브리프 — W2 rev1: 섭동 실험 (C1 인공물 측정 / C2 순위 불안정성)

> **대상:** Claude Code · **작성:** 2026-09-24 · **rev0(2026-09-23)를 대체한다.**
> **전제:** W1 완료. 게이트 1 통과 — MatrixProfile, `fit_on=train_prefix` AB-join, P1 79.2%.
> **원칙:** `BRIEF_W1.md` §1 비협상 원칙과 §7 STOP 조건을 그대로 적용한다 (`CLAUDE.md` 참조).
> **기간:** 2026-09-24 ~ 09-29 · **마감:** KIISE 포스터 논문 10/12. E2까지가 논문의 최소 단위다.
> **보고 언어:** 한국어. 코드·경로·로그·수치 식별자는 원문 그대로.
> **세션 분할:** 세션 1 = E0 → 표본 → E1 → 게이트 2. 세션 2 = 결정 기록 커밋 → E2 → E3 (→ E4). 두 세션 사이에 연구자 승인이 들어간다.

---

## 0. rev0 대비 변경 요약

1. **E0 정확성 기준**: `max|Δ| == 0.0` → 허용오차 `1e-6` + 실측 최댓값 보고. 대신 `resp_before`와 `resp_after`를 **같은 계산 경로**(`score_patch`)로 구한다 (§4).
2. **게이트 2 예산 기준**: "구간 위치 3종"(rev0 §5)과 "위치 20 × 길이 3"(rev0 §6)의 불일치를 후자로 통일 (§7).
3. **D12 예측 문구 수정**: 윈도우 점수 `s(j*)`(등식)와 `resp`(부등식)를 분리 (§3, D12).
4. **재구성형 연산자의 순환성**: 학습 구간에서 donor를 가져오면 AB-join 참조 집합을 그대로 붙이는 것이 된다. donor를 테스트 정상 구간에서 가져오는 `recon_test`를 주 연산자로, `recon_train`은 순환성 시연용 대조로 둔다 (§3, D11).
5. **SAR 재정의**: 이상 구간 점수를 **올리는** 연산자가 절댓값 SAR에서 보상받는 문제를 수정. 이상 구간 R과 분모 구간의 길이를 맞춘다 (§3, D10).
6. **E3 정의 보강**: 설명 대상 출력 `f`, attribution 영역, 세그먼트 단위, 순위 집계 방식을 명시 (§3, D13).
7. **자기 일치 설계 보강**: 설명용 연산자(`recon_test`)는 주 분석의 평가용 연산자 목록에서 제외 (§3, D-E3-1 rev).
8. **표본 추출 단위**: 시리즈 추출이 아니라 `content_group` 추출, 그룹당 1개 (§5).
9. **오염 방지 규칙 신설** (§12).

---

## 1. 이번 주에 답하는 질문

- **C1:** 마스킹 연산자가 **정상 구간**에서 이상 점수를 얼마나 움직이는가 (Artifact Index, AI). 이상 구간의 "제거 반응"과의 비 (SAR).
- **C2:** 설명기(AM) 순위가 **어떤 평가용 연산자를 썼느냐에 따라** 바뀌는가. (분류에서는 Šimić et al. 2025가 이미 보였다. 이번 주는 AD에서의 확인이다. 새로움을 주장하는 서술을 보고서에 쓰지 않는다.)
- **C3(축소):** GT 근접 순위는 plausibility 축의 기술 통계로만. 가설 검정하지 않는다.

---

## 2. 표기와 공통 정의

- 모든 구간은 D3(`matlab_1based_inclusive`)에서 **0-based 반열림 `[a, b)`**로 변환한 뒤 쓴다.
- `w` = 시리즈별 MP 윈도우 길이 (= stumpy의 `m`). `h = w // 2`.
- `support(j) = [j − h, j − h + w)` (W1 D-C5-5).
- **R에 걸치는 윈도우 집합** `J(R) = { j : support(j) ∩ R ≠ ∅ } = [a + h − w + 1, b + h − 1]` (유효 범위로 자름).
- **R의 영향 범위** `I(R) = [a − w + 1, b + w − 1)` = `J(R)` 윈도우들의 support 합집합. R을 섭동하면 이 범위 밖의 점수는 변하지 않는다.
- **제외 영역** `E = [GT_start − w, GT_stop + w)` (buffer = w, D-C2-1).
- `s_normal` = 테스트 구간에서 `E`를 뺀 영역의 점수 (D-C5-4). `scale(x) = q99(s_normal) − median(s_normal)` (D8).
- **정상 섭동 허용 조건**: `I(R) ⊂ 테스트 구간` 이고 `I(R) ∩ E = ∅`.
- **`j*(R)`**: `|R| = w`일 때 support가 R과 정확히 같은 윈도우, `j* = a + h`.

---

## 3. 연구자 결정

### 확정 (rev0에서 승인됨, 변경 없음)

- **D7** — 추적량 `resp(x, R) = max{ s(j) : j ∈ J(R) }`. P1과 AI에 같은 형태로 적용.
- **D8** — `AI(x, R, op) = ( resp(x_perturbed, R) − resp(x, R) ) / scale(x)`. `scale(x)`가 0이거나 비정상이면 그 시리즈를 제외하고 사유를 보고한다.
- **D9** — AI는 부호를 살려 저장·보고한다.

### 승인 대기 — PENDING

> **아래 항목은 연구자 승인 전이다.** 세션 1에서는 `DECISIONS.md`의 PENDING 섹션에 문구를 옮기는 것까지만 한다.
> 승인 후 세션 2 시작 시 **결정만 담은 커밋**을 먼저 만든다. 그 커밋 해시가 사전 등록의 타임스탬프다.
> 이 커밋 전에는 E2·E3를 실행하지 않는다.

**D10 — SAR 정의 (rev0 D9의 SAR 부분을 대체)**

- 이상 구간 `R_anom = [GT_start, GT_stop)` (GT 전체). `L = |GT|`.
- `AI_anom = AI(x, R_anom, op)`, 부호 유지.
- 제거 반응 `removal = max(0, −AI_anom)`. `AI_anom > 0`이면 `reversal = True`로 표기한다. 연산자가 이상 구간의 점수를 오히려 올린 경우다.
- 분모: 정상 섭동 허용 조건을 만족하는 **길이 L** 구간 20개(서로 겹치지 않음, seed 고정)의 `median |AI_normal_L|`.
- `SAR = removal / median|AI_normal_L|`.
- 분모 `< 1e-6`이면 `SAR = NaN`, `sar_den_zero = True`, 건수를 보고한다.
- 길이 L 구간이 10개 미만만 들어가면 `sar_few_positions = True`로 표기한다. 5개 미만이면 그 시리즈는 SAR에서만 제외한다 (E2a에서는 제외하지 않는다).
- **보조 지표** `SAR_abs = |AI_anom| / median|AI_normal_L|`(rev0 정의)를 나란히 보고한다. 정의 선택이 결론을 바꾸는지 보이기 위해서다.

**D11 — 재구성형 연산자의 donor 출처**

- **`recon_test` (주 연산자)**
  - donor 후보: 테스트 구간 안이면서 아래 영역과 겹치지 않는 길이 `|R|` 구간.
    - `E`
    - `I(R)`
    - (다중 구간 섭동이면) 다른 모든 구간의 `I(·)`
    - 가장자리 문맥 구간
  - 가장자리 문맥 길이 `c = max(1, w // 4)`.
  - 선택 기준: `x[a−c:a] ‖ x[b:b+c]`와 `x[u−c:u] ‖ x[u+L:u+L+c]`의 **원시값 유클리드 거리**가 최소인 `u`. 동률이면 가장 작은 `u`. 결정적이다.
  - donor는 **섭동 전 원본 x**에서 가져온다.
- **`recon_train` (대조, 순환성 시연)**
  - 같은 기준으로 donor를 **학습 구간**에서 가져온다.
  - 주 분석의 결론에는 쓰지 않는다. D12-b 검증과 "donor 출처가 AI를 결정한다"는 대조에만 쓴다.
- rev0의 "선형 보간" 선택지는 채택하지 않는다. 직선은 z-정규화하면 기울기와 무관하게 같은 모양이 되기 때문이다. B1–B6 안에 선형 보간이 있으면 메타데이터로만 표기한다 (§6).

**D12 — 사전 등록 예측 (rev0 §6의 D12-a를 대체)**

예측 대상은 `resp`가 아니라 **`j*` 윈도우의 점수**다. 허용오차 `tol`은 §4의 stumpy 소스 추적 결과로 정해 보고서에 적는다.

- **D12-a.** 연산자 메타데이터 `canonical_shape = constant`, `|R| = w`, `n_const_train = 0`이면:
  - (i) `s(j*)_after = √w` (등식, `|Δ| ≤ tol`)
  - (ii) `resp_after ≥ √w − tol` (부등식)
  - 근거: stumpy는 한쪽만 상수인 쌍을 `D² = m`으로 둔다(W1 D12, 소스 확인). z-정규화 거리 `D² = 2m(1 − ρ)`에서 이는 `ρ = 0.5`에 해당한다.
  - 주의: (i)은 신호 진폭과 무관하지만 AI는 무관하지 않다. `resp_before`와 `scale(x)`가 시리즈마다 다르기 때문이다. 보고서에서 이 둘을 섞지 않는다.
- **D12-a′.** 같은 조건에서 `n_const_train > 0`이면 `s(j*)_after = 0` (`|Δ| ≤ tol`).
- **D12-b.** `recon_train`, `|R| = w`이면 `s(j*)_after ≈ 0` (`≤ tol`). donor 윈도우가 참조 집합에 그대로 있기 때문이다.
- **판정**: 각 예측에서 조건을 만족하는 사례의 **1% 초과**가 어긋나면 "체계적 불일치"로 STOP한다 (§11).
- 위 조합 외에는 예측하지 않는다. 탐색적으로만 보고한다.

**D13 — E3의 설명 대상과 영역**

- 설명 대상 출력: `f(x) = resp(x, R_anom)`.
- attribution 영역: `Ω = I(R_anom) ∩ 테스트 구간`. `f`는 Ω 밖 입력에 **정확히 무관**하다(AB-join 구조). 따라서 Ω 밖 attribution은 정의하지 않는다.
- 세그먼트: 길이 `g = max(1, w // 4)`로 Ω를 분할한다. 세그먼트 수 `K > 64`이면 `g`를 키워 `K ≤ 64`로 맞추고, 시리즈별 `g`를 기록한다.
- 다중 세그먼트 마스킹: 가려진 세그먼트를 **최대 연속 구간(run)**으로 묶고, 각 run에 연산자를 적용한다.
- **AM별 정의**
  - `RandomAttribution`: 세그먼트별 균등 난수, seed 고정.
  - `FeatureAblation`: `f(x) − f(x with segment k masked)`, 설명용 연산자 사용.
  - `KernelSHAP`: 세그먼트 단위, 설명용 연산자로 마스킹. 표본 수 `S`는 E3 예산 측정 후 고정하고 기록한다.
  - `MPNative`: `resp`를 달성한 윈도우 `j_max`와 그 최근접 학습 이웃에 대해, 시점별 기여 `(ẑ_q,i − ẑ_nn,i)²`를 세그먼트로 합산한다. `j_max`의 support 밖은 0. **새로움을 주장하지 않는다.** 선행연구 확인은 연구자 측에서 한다 (§14).
- **충실성 곡선**
  - MoRF/LeRF를 세그먼트 비율 10단계(10%, 20%, …, 100%)로 계산한다.
  - `max_diff = scale(x)` (D8).
  - DDS/PES/CMI는 `faithfulness_simic.py`를 재사용한다.
- **AM 순위에 쓰는 지표**: Šimić et al. 저장소(commit `edc6a870`)에서 AM 순위 산출에 쓰인 지표를 **소스 추적해 동일하게** 쓴다. 추적 결과(파일:함수)를 기록한다. 임의로 고르지 않는다.
- **집계**
  - (주) 연산자별로 AM 지표를 `content_group` 평균 → AM 순위 → 연산자 쌍 간 Spearman ρ, 전체 Kendall's W.
  - `content_group` 단위 부트스트랩 B = 1000으로 신뢰구간을 낸다.
  - (보조) 그룹별 순위의 W 분포.
  - (보조) 연산자별 AM 쌍 비교는 Wilcoxon signed-rank + Holm.
  - AM이 4개라 순위 쌍의 ρ는 11개 값만 가진다. 보고서에 이 해상도 한계를 명시한다.

**D-E3-1 (rev) — 설명용/평가용 연산자 분리**

- 설명용 연산자는 `recon_test`로 고정한다.
- 주 분석의 평가용 연산자에서 `recon_test`는 **제외**한다 (자기 일치 칸 제거). `recon_train`도 같은 계열이므로 주 분석에서 제외하고 별도로 보고한다.
- **민감도 분석**: 평가용 연산자마다 "설명용 = 평가용"으로 FeatureAblation·KernelSHAP를 다시 계산한다. 주 조건 대비 순위 변화로 자기 일치 효과의 크기를 보고한다.

---

## 4. E0 — 증분 점수 계산 (최우선)

1. **`MatrixProfileDetector.score_patch(x, R) -> (J, scores)` 추가.**
   - `J(R)`의 윈도우만 학습 구간 참조에 대해 계산한다.
   - 사용하는 stumpy 경로(함수, 인자, 버전)를 소스 추적해 기록한다.
2. **`resp`는 전후 모두 `score_patch` 출력으로만 계산한다.** 기저 전체 점수를 `resp_before`에 쓰지 않는다. 같은 경로를 타야 수치 오차가 상쇄된다.
3. **정확성 테스트 (필수)**
   - 무작위 시리즈 20개 × 무작위 R 20개. 길이는 {0.25, 0.5, 1.0}w 중 무작위, R은 테스트 구간 안.
   - 각 R에 비항등 섭동(예: 가우시안 잡음)을 가한 `x'`에서, `score_patch(x', R)`와 전체 재계산의 `J(R)` 점수를 비교한다.
   - 기준: `max|Δ| ≤ 1e-6`. 실측 최댓값과 분포를 보고한다. 넘는 사례가 있으면 목록과 함께 STOP.
4. **항등 비트 일치**: 항등 연산자에 대해 `AI == 0.0`이 전 사례에서 정확히 성립해야 한다.
5. **stumpy 소스 추적 (D12의 `tol` 결정용)**
   - (a) 상수 서브시퀀스 판정 기준 (정확히 `ptp == 0`인지, 임계값이 있는지).
   - (b) 상관계수가 1에 가까울 때의 처리 (임계값, 거리 0으로 맞추는지).
   - (c) `D² = m` 분기의 위치.
   - 각각 파일:함수:버전으로 기록한다.
6. **속도 측정**: R 하나당 평균 시간을 `w`, `|R|`, 시리즈 길이별로 보고한다.

## 5. 실험 표본 (메타데이터만으로 추출, 결과를 보기 전에 커밋)

- **추출 단위는 `content_group`(W1 D-F2-2), 그룹당 시리즈 1개.** 그룹 안에서는 seed 고정 무작위로 고른다.
- 그룹 추출: `domain_goswami` 비례 층화, 도메인당 최소 1그룹. 도메인이 없는 그룹은 `other`.
- 기본 60그룹. 게이트 2에서 89그룹 전체의 예상 시간이 예산 안이면 **89그룹**(그룹당 1개)으로 올린다. 250 전수는 이번 주 범위 밖이다(민감도, W3 이후).
- 줄여야 하면 임의로 줄이지 말고 보고한다. 40그룹이 하한이다.
- 게이트 1 탐지 실패 시리즈도 포함하고 `detected` 플래그를 단다.
- 메타데이터를 산출한다: `n_const_train`(학습 구간의 길이 w 상수 서브시퀀스 수), `scale(x)`, `|GT|`, 정상 섭동 허용 위치 수(길이별).
- `configs/w2_sample.yaml`에 커밋한다. 사후 변경은 금지한다.

## 6. E1 — 마스킹 연산자 확정 (`configs/operators.yaml`)

- `experiments/exp_a_artifact.py`(legacy)의 B1–B6 정의를 **그대로** 가져오고, 원 정의를 표로 옮긴다.
- 추가 연산자: `recon_test`, `recon_train` (D11), `identity`.
- 인터페이스: `apply(x, runs, rng, ctx) -> x_perturbed`. `runs`는 `[a, b)` 목록, `ctx`는 `train_end`, `GT`, `w` 등. 원본 배열은 바꾸지 않는다.
- **메타데이터**
  - `canonical_shape` ∈ {`constant`, `linear`, `none`} — z-정규화 후 모양이 입력과 무관하게 고정되는지.
  - `uses_reference_set` (bool) — `recon_train`만 True.
  - `stochastic` (bool).
- 단위 테스트
  - 원본 불변.
  - `identity`는 비트 동일.
  - `recon_*`의 donor가 금지 영역과 겹치지 않음.
  - `constant` 연산자 출력의 표준편차가 0.

## 7. 게이트 2 — 세션 1의 종료 조건

1. E0 정확성 테스트 통과 (`max|Δ| ≤ 1e-6`).
2. 항등 연산자 AI가 전 사례에서 정확히 0.0.
3. **E2 전체 격자의 예상 시간이 2시간 이하** (Mac 4워커).
   - E2 전체 격자 = 표본 × 연산자 9종 × (정상 위치 20 × 길이 3 + SAR용 길이 L 위치 20 + 이상 구간 1).
   - 5개 시리즈로 실측해 외삽한다.
   - **이 측정에서 계산된 AI 값은 저장·출력하지 않는다** (§12).
   - 60그룹과 89그룹 각각의 예상치를 보고한다.

하나라도 실패하면 STOP 후 보고한다. 3번이 실패해도 표본을 임의로 줄이지 않는다.

산출물: `reports/w2_gate2.md`.

## 8. E2 — C1: Artifact Index와 SAR (세션 2, 결정 커밋 이후)

**E2a — AI 특성화**
- 시리즈당 정상 위치 20개 × 길이 {0.25, 0.5, 1.0}w (반올림, 최소 1) × 연산자 전체.
- 위치는 정상 섭동 허용 조건 안에서, seed 고정, R끼리 겹치지 않게 뽑는다.
- `|R| = w`인 경우 `s(j*)_after`를 따로 저장한다 (D12 검증용).

**E2b — SAR (D10)**
- 이상 구간 `R_anom` 1개 + 길이 `|GT|`인 정상 구간 20개 × 연산자 전체.

**산출물**
- `results/w2/ai.parquet` — 열: `series_id, content_group, domain, detected, n_const_train, op, canonical_shape, region, L_rel, pos_idx, a, b, resp_before, resp_after, s_jstar_after, scale, AI`
- `results/w2/sar.parquet`
- `reports/w2_artifact.md`
  - 연산자별 AI 분포(부호 유지)
  - D12 예측 대조표 (조건별 사례 수, 일치율, 불일치 목록)
  - SAR과 SAR_abs 나란히, reversal 비율
  - `detected` 분할, 도메인별 분할
  - 플래그 시리즈를 뺀 결과와 넣은 결과 **둘 다**
  - `recon_test` 대 `recon_train` 대조
- `figures/w2/ai_by_operator.png`

## 9. E3 — C2: 설명기 순위 불안정성 (세션 2, E2 완료 후)

- 정의는 D13, D-E3-1(rev)을 따른다.
- `faithfulness_ad.py` 스텁을 D7–D9, D13으로 채운다.
- **예산 측정**: 3개 시리즈로 실측·외삽해 주 분석 + 민감도 분석의 예상 시간을 낸다.
  - 8시간(야간 1회) 이하면 진행한다.
  - 넘으면 KernelSHAP `S`와 예상 시간표를 들고 STOP한다.
- **산출물**
  - `results/w2/faithfulness.parquet`
  - `reports/w2_ranking.md` — 주 분석, 자기 일치 민감도, AM 순위 지표의 소스 추적 결과, ρ 해상도 한계 명시
  - `figures/w2/rank_bump.png`

## 10. E4 — C3 축소판 (E3 완료 후 여유가 있을 때만)

- GT 구간과 attribution의 겹침으로 AM 순위를 낸다. 지표 이름은 Relevance Mass Accuracy / Relevance Rank Accuracy(Arras et al. 2022)를 쓴다.
- E3 순위와의 Kendall's τ를 **기술 통계로만** 보고한다. 보고서에서 "plausibility"로 부르고 faithfulness와 구분한다.

## 11. STOP 조건 (W1 §7에 추가)

- E0 정확성 기준 초과 (`max|Δ| > 1e-6`)
- 게이트 2 세 조건 중 하나라도 실패
- `scale(x)` 이상으로 제외되는 시리즈가 표본의 10% 초과
- `recon_test`가 donor를 찾지 못하는 R이 5% 초과
- D12 예측 중 어느 하나라도 해당 사례의 1% 초과가 불일치 — 해석을 만들지 말고 불일치 목록을 보고
- E3 예상 시간 8시간 초과
- D10–D13 결정 커밋이 없는 상태에서 E2·E3 실행이 필요해지는 경우

## 12. 오염 방지 (사전 등록 위생)

- 결정 커밋 전에는 실제 연산자로 AI·충실성 값을 계산해 **저장·출력·요약하지 않는다.**
- 예외는 두 가지다: E0 정확성 테스트(점수 비교이지 AI가 아님), 게이트 2 속도 측정(값은 버림).
- rev0 기준으로 이미 계산된 AI·충실성 결과 파일이 있으면 **열지 말고** 경로·생성 시각만 보고한다.

## 13. 완료 정의

**세션 1**
- [ ] 0단계 상태 보고 (git, 기존 결과 파일 유무, env 정리 D-D3-1, Wafer zero class 결과 유무)
- [ ] `score_patch` + 정확성 테스트 + 항등 비트 일치 + 속도 보고
- [ ] stumpy 소스 추적 3건 (§4-5)
- [ ] `configs/w2_sample.yaml` 커밋
- [ ] `configs/operators.yaml` + 단위 테스트
- [ ] `DECISIONS.md` PENDING 섹션에 D10–D13, D-E3-1(rev) 문구 이관 (승인 표기 금지)
- [ ] `reports/w2_gate2.md`

**세션 2**
- [ ] 연구자 승인 문구대로 결정 기록 → **결정 전용 커밋**, 해시 보고
- [ ] E2 → `reports/w2_artifact.md` + 그림
- [ ] E3 예산 측정 → E3 → `reports/w2_ranking.md` + 그림
- [ ] (여유 시) E4

## 14. 이 브리프 밖 — 연구자 측에서 처리 (Claude Code는 검색·주장하지 않음)

- `MPNative`와 같은 분해가 Matrix Profile 기반 이상 설명에 이미 있는지
- arXiv:2604.17616이 OOD 섭동 문제를 **정량화**했는지 (C1의 "first quantification" 주장 수위 결정)
- PM8 방향 문제(구멍 2)의 비대칭 메커니즘 — 이번 주에는 PM8에 관한 예측을 등록하지 않는다
